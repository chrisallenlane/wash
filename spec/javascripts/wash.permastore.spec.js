/*
describe("wash.permastore", function() {
    // because I don't want testing to erase stored connection,
    // buffer stored connections before running the tests
    // @bug: this doesn't seem to actually be loading. Is some framework
    // interfering with localStorage access?
    wash.permastore.init();
    var buffered_storage = wash.permastore.storage;
    console.log(buffered_storage);
});
*/
